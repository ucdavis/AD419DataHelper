@{
    var title = "LogOn";

    var queryString = Request.Url.Query;
    if (String.IsNullOrEmpty(queryString) == false)
    {
        queryString = Server.UrlDecode(queryString).Substring(queryString.IndexOf("?") + 1);

        //System.Collections.Specialized.NameValueCollection nvc = new System.Collections.Specialized.NameValueCollection();
        //string[] sections = Server.UrlDecode(queryString).Split(new char[] { '&' });

        //foreach (string section in sections)
        //{
        //    string[] pair = section.Split(new char[] { '=', '/' });
        //    nvc.Add(pair[0].ToString(), pair[pair.Length - 1].ToString());
        //}
        //title = nvc["ReturnUrl"];

        title = queryString.Substring(queryString.LastIndexOf("/") + 1);
    }

    <br />
    ViewBag.Title = title;
}
<h3>
    You do not have access to view this page
</h3>
